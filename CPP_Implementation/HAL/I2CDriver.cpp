#include "I2CDriver.hpp"

bool I2CDriver::init() {
    return true;
}

bool I2CDriver::write(uint8_t address, uint8_t* data, size_t length) {
    return true;
}

bool I2CDriver::read(uint8_t address, uint8_t* data, size_t length) {
    return true;
}
